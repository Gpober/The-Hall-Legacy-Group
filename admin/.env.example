# Supabase — Hall Legacy CRM
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx

# Optional. Serve the app under a sub-path instead of the domain root.
# Set to "/admin" when proxying from thehalllegacygrp.com/admin.
# Leave UNSET (blank) when serving at a domain root such as
# admin.thehalllegacygrp.com or the *.vercel.app URL.
# NEXT_PUBLIC_BASE_PATH=/admin
